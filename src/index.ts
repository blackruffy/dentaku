import { App } from './app';
import { render } from './state';

render(App, {
  command: '',
  isCleared: false,
  resultAnimation: '',
  dialogBody: '',
  dialogLeft: '100vw',
  dialogAnimation: '',
});
